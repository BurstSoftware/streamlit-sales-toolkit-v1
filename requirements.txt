streamlit==1.26.0
pandas==2.1.1
re==2023.8.0  # Note: 're' is a standard library in Python, typically not installed via pip
sendgrid==6.10.0
hashlib==2023.9.0  # Note: 'hashlib' is also a standard library, not installed via pip
uuid==1.30  # Note: 'uuid' is a standard library, not installed via pip
